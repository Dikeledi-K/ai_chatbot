const state = { feature: 'chat', busy: false, quizScore: 0 };
const featureNames = { chat: 'Study chat', explain: 'Explain a topic', summarize: 'Summarise notes', planner: 'Study planner', quiz: 'Quiz me', assignment: 'Assignment helper' };
const form = document.querySelector('#chat-form');
const input = document.querySelector('#message-input');
const messages = document.querySelector('#messages');
const sendButton = document.querySelector('#send-button');
const statusText = document.querySelector('#status-text');

function escapeHtml(value) { return value.replace(/[&<>'"]/g, (character) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#039;', '"': '&quot;' })[character]); }
function formatAnswer(value) { return escapeHtml(value).replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>').replace(/^### (.*)$/gm, '<h3>$1</h3>').replace(/^- (.*)$/gm, '<li>$1</li>').replace(/(?:<li>.*<\/li>\n?)+/g, (list) => `<ul>${list}</ul>`).replace(/\n/g, '<br>'); }
function addMessage(content, role = 'user', loading = false) { const item = document.createElement('div'); item.className = `message ${role}${loading ? ' loading-message' : ''}`; item.innerHTML = role === 'assistant' ? `<div class="avatar">✦</div><div class="bubble">${loading ? '<span class="typing"><i></i><i></i><i></i></span>' : `<div>${formatAnswer(content)}</div>`}</div>` : `<div class="bubble">${escapeHtml(content).replace(/\n/g, '<br>')}</div>`; messages.append(item); messages.scrollTop = messages.scrollHeight; return item; }
function setBusy(value) { state.busy = value; sendButton.disabled = value; input.disabled = value; statusText.textContent = value ? 'Thinking...' : 'Ready to learn'; document.querySelector('.status-dot').classList.toggle('working', value); }
function selectFeature(feature) { state.feature = feature; document.querySelectorAll('.feature-card').forEach((card) => card.classList.toggle('active', card.dataset.feature === feature)); document.querySelector('#mode-label').textContent = feature === 'chat' ? 'STUDY CHAT' : featureNames[feature].toUpperCase(); document.querySelector('#mode-title').textContent = feature === 'chat' ? 'What are you working on?' : `${featureNames[feature]} with confidence.`; input.placeholder = feature === 'summarize' ? 'Paste your notes here...' : feature === 'explain' ? 'What topic should we unpack?' : 'Ask a question or paste your notes...'; }
async function sendMessage(message, feature = state.feature) { if (state.busy || !message.trim()) return; setBusy(true); addMessage(message); const loading = addMessage('', 'assistant', true); try { const response = await fetch('/api/chat', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ message, feature }) }); const result = await response.json(); loading.remove(); if (!response.ok) throw new Error(result.error || 'Something went wrong.'); addMessage(result.answer, 'assistant'); } catch (error) { loading.remove(); addMessage(error.message, 'assistant'); } finally { setBusy(false); input.focus(); } }

form.addEventListener('submit', (event) => { event.preventDefault(); const value = input.value.trim(); input.value = ''; sendMessage(value); });
input.addEventListener('keydown', (event) => { if (event.key === 'Enter' && !event.shiftKey) { event.preventDefault(); form.requestSubmit(); } });
document.querySelectorAll('.feature-card').forEach((card) => card.addEventListener('click', () => { const feature = card.dataset.feature; selectFeature(feature); if (feature !== 'chat' && ['planner', 'quiz', 'assignment'].includes(feature)) openFeatureModal(feature); else input.focus(); }));
document.querySelectorAll('[data-suggestion]').forEach((button) => button.addEventListener('click', () => { selectFeature('chat'); input.value = button.dataset.suggestion; input.focus(); }));
document.querySelector('#clear-chat').addEventListener('click', () => { messages.innerHTML = '<div class="message assistant"><div class="avatar">✦</div><div class="bubble"><p>Fresh page, fresh thinking. What should we work on?</p></div></div>'; state.quizScore = 0; });

const modal = document.querySelector('#feature-modal');
const modalForm = document.querySelector('#feature-form');
function openFeatureModal(feature) { const labels = { planner: ['STUDY PLANNER', 'Build a plan that fits your week.'], quiz: ['QUIZ ME', 'Turn revision into a quick challenge.'], assignment: ['ASSIGNMENT HELPER', 'Understand the task before you start.'] }; document.querySelector('#modal-kicker').textContent = labels[feature][0]; document.querySelector('#modal-title').textContent = labels[feature][1]; const templates = { planner: `<label>Subject<input name="subject" placeholder="e.g. Biology" required></label><label>Exam date<input name="examDate" type="date" required></label><label>Number of topics<input name="topics" type="number" min="1" max="100" placeholder="e.g. 8" required></label><label>Study time per week<input name="hours" type="number" min="1" max="80" placeholder="e.g. 4" required></label><button class="primary-action" type="submit">Create my plan <span>→</span></button>`, quiz: `<label>Quiz topic<input name="topic" placeholder="e.g. Fractions" required></label><p class="form-note">StudyBuddy will ask one question at a time so you can practise, not just peek at answers.</p><button class="primary-action" type="submit">Start quiz <span>→</span></button>`, assignment: `<label>Assignment question<textarea name="question" rows="5" placeholder="Paste the question or brief here..." required></textarea></label><button class="primary-action" type="submit">Break it down <span>→</span></button>` }; modalForm.innerHTML = templates[feature]; modal.classList.remove('hidden'); modalForm.querySelector('input, textarea').focus(); }
function closeModal() { modal.classList.add('hidden'); }
document.querySelector('#modal-close').addEventListener('click', closeModal); modal.addEventListener('click', (event) => { if (event.target === modal) closeModal(); });
modalForm.addEventListener('submit', async (event) => { event.preventDefault(); const feature = state.feature; const data = Object.fromEntries(new FormData(modalForm)); const button = modalForm.querySelector('button[type="submit"]'); button.disabled = true; button.textContent = 'Preparing...'; try { const response = await fetch('/api/feature', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ feature, data }) }); const result = await response.json(); if (!response.ok) throw new Error(result.error); closeModal(); await sendMessage(result.message, feature); } catch (error) { modalForm.insertAdjacentHTML('beforeend', `<p class="form-error">${escapeHtml(error.message)}</p>`); button.disabled = false; button.innerHTML = 'Try again <span>→</span>'; } });
